% Geombinatorics build: 11pt Times, 5.5 x 8.5 inch landscape, no page numbers.
% The manuscript itself is joining-rotations.tex; this file only picks the format,
% so the two outputs can never drift apart.
\def\geombinatorics{}
\input{joining-rotations}
